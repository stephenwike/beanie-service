apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ .Values.ingress }}
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: "beanie.fireshellstudio.com"
    http:
      paths:
      - path: "/"
        backend:
          serviceName: {{ .Values.webapi }}
          servicePort: 80

